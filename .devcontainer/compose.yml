services:
  python:
    build:
      dockerfile: Dockerfile
      context: python/
      args:
        - UID
        - GID
    tty: true
    stdin_open: true
    restart: always
    volumes:
      - ..:/workspace
